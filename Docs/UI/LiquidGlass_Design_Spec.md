# Legacy Design Spec: Liquid Glass (Archived)

This document captured an earlier Liquid Glass/Neon UI concept. The current
implementation is Canvas-first and flat, and Liquid Glass tokens/modifiers are
not part of the live UI.

**Source of truth for current UI status:** `Docs/REALITY_MAP.md`

## Notes
- Keep this spec for historical context only.
- Current UI entry point: `llmHub/Views/UI/RootView.swift` (`CanvasRootView`).
- Current styling uses `AppColors` and matte surfaces.
