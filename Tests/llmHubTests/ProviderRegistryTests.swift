// This file is intentionally empty.
// Rationale: The Xcode project uses a file-system-synchronized "Tests" folder that can
// accidentally add new files to the main app target. Keeping this guarded avoids build breaks.
#if canImport(XCTest)
import XCTest
#endif
